<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width,initial-scale=1,maximum-scale=1"
    />
    <title>{{subject}}</title>
    <style>
        .confirm-button {
            display: inline-block;
            padding-left: 30px;
            padding-right: 30px;
            font-size: 18px;
            letter-spacing: normal;
            background-color: #43b02a;
            border: 0;
            border-radius: 8px;
            color: white;
            text-transform: uppercase;
        }

        .confirm-button__inner {
            word-break: break-word;
            line-height: 36px;
        }
    </style>
  </head>
  <body>
    <div class="root">
        {{{body}}}

        <br/>

        <a class="confirm-button" href="{{confirmationLink}}">
            <span class="confirm-button__inner">Confirm subscription</span>
        </a>
    </div>
  </body>
</html>